import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Tournaments | Text Golf' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
